# Skiff Homebrew tap

Homebrew formula for [Skiff](https://github.com/nuelScript/skiff) — ship it to a
server you own.

```sh
brew install nuelScript/tap/skiff
```

The formula is updated automatically on each Skiff release. See the
[main repo](https://github.com/nuelScript/skiff) for docs.
